---
import Layout from '../layouts/Layout.astro';

const orderID = Astro.url.searchParams.get('orderID');
---

<Layout>
	<div class="result-page canceled">
		<section class="result-card" aria-label="Pago cancelado">
			<p class="tag" data-i18n="cancelTag">Pago cancelado</p>
			<h1 data-i18n="cancelTitle">Tu pago no se completó</h1>
			<p data-i18n="cancelLead">
				No te preocupes, no se realizó ningún cobro exitoso. Puedes intentarlo nuevamente cuando
				quieras.
			</p>
			{orderID && <p class="order"><span data-i18n="cancelOrderPrefix">Orden cancelada:</span> {orderID}</p>}
			<div class="actions">
				<a href="/compra" class="btn primary" data-i18n="cancelButton1">Intentar de nuevo</a>
				<a href="https://wa.me/18438226879" target="_blank" rel="noreferrer noopener" class="btn secondary" data-i18n="cancelButton2">Hablar por WhatsApp</a>
			</div>
		</section>
	</div>
</Layout>

<style>
	.result-page {
		display: flex;
		justify-content: center;
		padding: 2rem 1rem;
	}

	.result-card {
		width: min(720px, 100%);
		border: 1px solid color-mix(in srgb, var(--pizarra-profunda) 15%, transparent);
		border-radius: 1rem;
		padding: 1.5rem;
		background: var(--blanco-editorial);
	}

	.tag {
		margin: 0;
		display: inline-flex;
		padding: 0.3rem 0.75rem;
		border-radius: 999px;
		font-size: 0.8rem;
		font-weight: 700;
		font-family: 'Montserrat', sans-serif;
		background: color-mix(in srgb, var(--terracota-solene) 20%, transparent);
		color: var(--terracota-solene);
	}

	h1 {
		margin: 0.75rem 0 0;
		font-size: clamp(1.6rem, 3.3vw, 2.2rem);
	}

	p {
		margin: 0.75rem 0 0;
		line-height: 1.6;
		color: var(--carbon-cinematografico);
	}

	.order {
		font-weight: 700;
	}

	.actions {
		margin-top: 1.5rem;
		display: flex;
		gap: 0.6rem;
		flex-wrap: wrap;
	}

	/* Buttons are styled via global .btn classes */
	.btn {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		text-decoration: none;
		padding: 0.8rem 1.75rem;
		border-radius: 999px;
		font-weight: 700;
		border: 2px solid transparent;
		transition: transform 0.2s ease;
		font-family: 'Montserrat', sans-serif;
	}
	
	.btn:hover {
		transform: scale(1.03);
	}

	.btn.primary {
		background: var(--oro-kodak);
		color: var(--carbon-cinematografico);
	}

	.btn.secondary {
		background: transparent;
		border-color: var(--pizarra-profunda);
		color: var(--pizarra-profunda);
	}
</style>